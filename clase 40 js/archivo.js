// Hacer una funcion que resiva una frace y devuelva el numero de vocales que tiene la misma

const myFunc = (frase) => {
    const arr = frace.split('');
    let count = 0;

    Array.forEach(letra => {
        if ()
    })
}
const input = prompt("Digame una frase");
myFunc(input);